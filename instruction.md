Tasks :

1. Data Collection and Cleaning
Collect all the datasets(customer data, churn history, time-based features)(all on the website)
Clean the missing values, duplicates and recheck the formatting 
Create a clean dataset in CSV format

2. Feature Engineering 
Create time aware features(recency, frequency, duration based behaviour)
encode categorical variables
create a final feature matrix ready for modelling

3. EDA
Perform univariate/bivariate analysis
Identify churn trends and correlations
prepare EDA summary slides + visualizations

4. Model Development 
Build and train churn prediction models(start with logistic regression, then  random forest if you get time because it is time consuming)
Document all model parameters
So you have to prepare _ trained model, code, performance metrics

5. Model Evaluation
Evaluate using Accuracy, precision, recall, ROC - AUC
Compare the models and select the final one
Create a Evaluation report and choose model justification

6. Business Translation
Convert the model outputs into business insights(eg high-risk customer groups, potential ROI)
write 1 page executive summary